<?xml version="1.0" encoding="utf-8"?>
<Window
  x:Class="MarketCountdownApp.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="Market Countdown"
  WindowStyle="None"
  ResizeMode="NoResize"
  Width="800"
  Height="430"
  Background="Transparent"
  AllowsTransparency="True">

    <!-- Panel grey background -->
    <Border Background="#B0B0B0" CornerRadius="15" SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="240"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 2x2 Grid of Market Timers -->
            <UniformGrid Rows="2" Columns="2" Margin="10" Grid.Row="0">

                <!-- London (open / blue ring) -->
                <Border Background="#FFFFFF" CornerRadius="12" Margin="5">
                    <Grid>
                        <Image
                            Source="pack://siteoforigin:,,,/Icons/London.png"
                            Width="32" Height="32"
                            Margin="10"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Left"/>
                        <StackPanel Margin="60,5,10,5">
                            <TextBlock Text="London" Foreground="#222222" FontSize="14"/>
                            <TextBlock Text="{Binding LondonOffset}" Foreground="#222222" FontSize="24"/>
                        </StackPanel>
                        <Ellipse Stroke="#409EFF"
                     StrokeThickness="6"
                     Width="60" Height="60"
                     VerticalAlignment="Top"
                     HorizontalAlignment="Right"
                     Margin="0,5,10,0"/>
                    </Grid>
                </Border>

                <!-- New York (closed / red ring) -->
                <Border Background="#FFFFFF" CornerRadius="12" Margin="5">
                    <Grid>
                        <Image
                            Source="pack://siteoforigin:,,,/Icons/New_York.png"
                            Width="32" Height="32"
                            Margin="10"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Left"/>
                        <StackPanel Margin="60,5,10,5">
                            <TextBlock Text="New York" Foreground="#222222" FontSize="14"/>
                            <TextBlock Text="{Binding NewYorkOffset}" Foreground="#222222" FontSize="24"/>
                        </StackPanel>
                        <Ellipse Stroke="#EB5757"
                     StrokeThickness="6"
                     Width="60" Height="60"
                     VerticalAlignment="Top"
                     HorizontalAlignment="Right"
                     Margin="0,5,10,0"/>
                    </Grid>
                </Border>

                <!-- Sydney (closed / orange ring) -->
                <Border Background="#FFFFFF" CornerRadius="12" Margin="5">
                    <Grid>
                        <Image
                            Source="pack://siteoforigin:,,,/Icons/Sydney.png"
                            Width="32" Height="32"
                            Margin="10"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Left"/>
                        <StackPanel Margin="60,5,10,5">
                            <TextBlock Text="Sydney" Foreground="#222222" FontSize="14"/>
                            <TextBlock Text="{Binding SydneyOffset}" Foreground="#222222" FontSize="24"/>
                        </StackPanel>
                        <Ellipse Stroke="#F08C00"
                     StrokeThickness="6"
                     Width="60" Height="60"
                     VerticalAlignment="Top"
                     HorizontalAlignment="Right"
                     Margin="0,5,10,0"/>
                    </Grid>
                </Border>

                <!-- Tokyo (closed / red ring) -->
                <Border Background="#FFFFFF" CornerRadius="12" Margin="5">
                    <Grid>
                        <Image
                            Source="pack://siteoforigin:,,,/Icons/Tokyo.png"
                            Width="32" Height="32"
                            Margin="10"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Left"/>
                        <StackPanel Margin="60,5,10,5">
                            <TextBlock Text="Tokyo" Foreground="#222222" FontSize="14"/>
                            <TextBlock Text="{Binding TokyoOffset}" Foreground="#222222" FontSize="24"/>
                        </StackPanel>
                        <Ellipse Stroke="#EB5757"
                     StrokeThickness="6"
                     Width="60" Height="60"
                     VerticalAlignment="Top"
                     HorizontalAlignment="Right"
                     Margin="0,5,10,0"/>
                    </Grid>
                </Border>

            </UniformGrid>

            <!-- "UP NEXT" List (white background) -->
            <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="12" Margin="10">
                <StackPanel>
                    <TextBlock Text="UP NEXT"
                     Foreground="#222222"
                     FontSize="16"
                     FontWeight="Bold"
                     Margin="10,5"/>
                    <ListView ItemsSource="{Binding UpcomingEvents}" Margin="10">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <TextBlock Text="{Binding Time}" Foreground="#222222" Width="80"/>
                                    <TextBlock Text="{Binding Currency}" Foreground="#222222" Width="80"/>
                                    <TextBlock Text="{Binding Description}" Foreground="#222222"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

        </Grid>
    </Border>
</Window>